#include <bits/stdc++.h>
#define rep(i,a,n) for(int i=a;i<(a+n);i++)
#define var auto
using namespace std;
using ll = long long;

int main() {
  int n,m,k;
  cin >> n >> m >> k;
  int a[m], b[m], c[k], d[k];
  rep(i,0,m) {
    cin >> a[i] >> b[i];
  }
  rep(i,0,k) {
    cin >> c[i] >> d[i];
  }

  int ans[n];
  rep(i,0,n) ans[i] = n-1;

  rep(i,0,m) {
    // a <-> b
    ans[a[]]
  }

  return 0;
}
